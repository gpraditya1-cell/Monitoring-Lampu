# Backend (Express API server — artifacts/api-server)
PORT=5000
DATABASE_URL=postgresql://username:password@localhost:5432/monitoring_lampu
# Replace username:password with your actual PostgreSQL credentials
NODE_ENV=development
LOG_LEVEL=info

# Frontend (Vite — artifacts/mhl) has its own .env.local file
# See artifacts/mhl/.env.example for frontend environment variables

# Supabase credentials belong in artifacts/mhl/.env.local
# Copy artifacts/mhl/.env.example and replace placeholder values with real Supabase credentials
